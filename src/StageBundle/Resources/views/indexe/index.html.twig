{% extends "layout.html.twig" %}
{% block head %}
    <title> {% block title %}{{ parent () }}-Forum{% endblock %}</title>
{% endblock %}
    {% block stylesheet %}
        <link rel="stylesheet" herf="{{ asset('css/style1good.css') }}" />

    {% endblock %}



     {% block content %}
<div class="content">
    <div class="animated fadeIn">
        <div class="row">

            <div class="col-md-12">
                <div class="card">
    <h1>Indexes list</h1>

    <table id="bootstrap-data-table" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Description</th>
                <th>Actions</th>
                <th>Articles</th>

            </tr>
        </thead>
        <tbody>
        {% for indexe in indexes %}
            <tr>
                <td><a href="{{ path('indexe_show', { 'id': indexe.id }) }}">{{ indexe.id }}</a></td>
                <td>{{ indexe.libelle }}</td>
                <td>{{ indexe.description }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('indexe_show', { 'id': indexe.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('indexe_edit', { 'id': indexe.id }) }}">edit</a>
                        </li>
                        <li> <a href="{{path('indexe_delete',{ 'id':indexe.id} )}}">delete</a>
                        </li>
                    </ul>
                </td>
                <td> <li><a href="{{path('test',{ 'id':indexe.id,'page':1} )}}">article</a></li>
               
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('indexe_new') }}">Create a new indexe</a>
        </li>
    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}