{% extends "layout.html.twig" %}

{% block content %}
    <h1>Indexe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ indexe.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ indexe.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ indexe.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('indexe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('indexe_edit', { 'id': indexe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
